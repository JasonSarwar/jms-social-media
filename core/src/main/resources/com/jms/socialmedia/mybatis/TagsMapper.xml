<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jms.socialmedia.mybatis.TagsMapper">

    <!-- insert id="addTags">
    	insert into posts_tags (post_id, tag) values
    	<foreach item="tag" collection="tags" separator=",">
    		(#{postId}, #{tag})
    	</foreach>
    </insert-->

	<select id="getTags" resultType="String">
        select distinct tag from posts_tags
    </select>

    <select id="getPostTags" parameterType="int" resultType="String">
        select tag from posts_tags
        where post_id = #{id}
    </select>

    <insert id="addTags">
    	insert all
    	<foreach item="tag" collection="tags">
    		into posts_tags (post_id, tag) values
    		(#{postId}, #{tag})
    	</foreach>
    	select * from dual
    </insert>

	<delete id="removePostTags">
		delete from posts_tags
		where post_id = #{id}
	</delete>

</mapper>